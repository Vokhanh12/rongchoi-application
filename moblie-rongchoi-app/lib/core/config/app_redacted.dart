import 'package:redacted/redacted.dart';

class AppRedacted {
  static RedactedConfiguration redactedConfiguration = RedactedConfiguration(
    animationDuration: const Duration(milliseconds: 800),
  );
}
